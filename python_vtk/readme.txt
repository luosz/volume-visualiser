Installation
1. Set the HTTP_RPOXY environment variable to the following proxy address:
HTTP_RPOXY=http://www-proxy.scss.tcd.ie:8080
2. Download and install Anaconda (a scientific Python distribution) from http://continuum.io/downloads
3. Run "conda install vtk" in Anaconda Command Prompt on Windows (or an equivalent terminal on Linux etc.)